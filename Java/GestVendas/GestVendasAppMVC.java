import java.io.Serializable;
import java.io.IOException;
import static java.lang.System.out;
/**
* Classe agregadora do projeto
**/
public class GestVendasAppMVC implements Serializable
{/*
  public static void main(String[] args) 
  {
      
        IGestVendasModel model = new GestVendasModel();
        try
        {   
            model.createData();
        }catch(IOException e){e.printStackTrace();out.println("ERRO INICIALIZACAO"); System.exit(-1); }
        
        //if(model == null) { out.println("ERRO INICIALIZACAO"); System.exit(-1); }
        IGestVendasView view = new GestVendasView();
        //Imprimir o menu que escolhe o ficheiro
        IGestVendasController control = new GestVendasController();
        control.setModel(model);
        control.setView(view); 
        control.startController();
        System.exit(0); 
    }  */
}

